{
    "builds" : [{
        "src" : "project/wsgi.py",
        "use" : "@vercel/python",
        "config" : {"maxLambdSize" : "15mb" , "runtime": "python3.13.2"}
    }],
    "routes" : [
        {
            "src": "/(.*)",
            "dest": "project/wsgi.py"
        }
    ]
}